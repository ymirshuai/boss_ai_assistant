# 使用注意事项

1. 本工具会实际控制你的手机在 BOSS 直聘上浏览岗位、发送打招呼与自动回复消息，属于真实操作，请务必在本人手机上、本人知情下使用。
2. 频繁打招呼可能触发 BOSS 直聘的频率限制或风控，请合理设置「每日目标打招呼数」与「每小时上限」（见 config.BEHAVIOR_CONFIG）。
3. 自动回复文案由大模型生成，重要沟通请人工复核，避免误发。
4. 运行前请确认手机已通过 USB 调试连接、ADB 可识别；否则循环会在重连处持续报错。
5. 「停止」按钮会发送停止信号，但正在执行的单次设备动作（如一次打招呼）会完成后才退出。
6. 视觉（看图）能力依赖多模态模型（qwen3.8-max），仅作 OCR 兜底；若不可用，字段会降级为「未识别到」，不影响主流程。
7. Agent 对话模块尚未接入，当前消息框为预留位，发送后会返回占位回复。
