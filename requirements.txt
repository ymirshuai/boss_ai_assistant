# ============================================================
# BOSS 直聘自动化助手 - 依赖清单 (requirements.txt)
# 适用运行环境: Python 3.10.11 (F:\BOSS_auto\.venv, Windows)
# 安装命令:     F:\BOSS_auto\.venv\Scripts\python.exe -m pip install -r requirements.txt
# 说明: 版本号取自实际运行环境 F:\BOSS_auto\.venv 的 pip freeze;
#       其中 pyperclip 在该环境实测未安装, 已标注需补装。
# ============================================================

# ---------- 核心第三方依赖 (代码直接 import) ----------
airtest==1.4.3
opencv-contrib-python==4.6.0.66      # 代码 import cv2
requests==2.34.2
dashscope==1.26.2                    # 阿里云百炼/MaaS: 多模态 OCR 与 RAG 调用
nltk==3.9.4                          # load_key.py 顶层 import
pyperclip>=1.8.2                     # device.py: 剪贴板复制文本到设备 (当前运行环境缺失, 需补装)

# ---------- airtest 传递依赖 (锁定实际环境版本, 确保可复现) ----------
numpy==1.26.4
pillow==12.3.0
six==1.16.0
websocket-client==1.9.0

# ---------- 已剔除的死依赖 ----------
# future==1.0.0  # 仅 job_browser.py 一处误写 import ("from future.backports.email.mime import message")
#                且 message 全程未使用, 已从代码删除 (job_browser.py:8), 切勿安装。
#                注意: 若 F:\BOSS_auto 运行副本仍保留该 import 行, 需同步删除, 否则按本清单安装后会报 ModuleNotFoundError: future。
