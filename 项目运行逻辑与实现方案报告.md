# BOSS 直聘 AI 自动助手 —— 运行逻辑与实现方案报告

> 报告日期：2026-07-10
> 适用范围：`boss_ai_assistant` 工作空间全部源码
> 阅读对象：项目负责人 / 维护开发者

---

## 一、项目定位与目标

这是一个**基于 ADB + 千问（Qwen）多模态大模型**的 BOSS 直聘安卓端自动化助手。它通过 Airtest 框架驱动真机，模拟人工操作，实现三类核心动作：

1. **自动浏览岗位** —— 截图后由视觉大模型识别岗位卡片，判断与求职期望的匹配度。
2. **自动打招呼** —— 匹配成功后自动生成打招呼文案并发送。
3. **自动回复消息** —— 识别 HR 消息意图，生成回复、按需发送简历 / 微信。

设计目标是在**不违反平台风控基本约束**（每日 / 每小时打招呼上限、最小间隔）的前提下，把「刷岗位 + 聊 HR」这类重复劳动自动化。

---

## 二、技术栈

| 维度 | 选型 | 说明 |
|------|------|------|
| 运行环境 | Python 3.11 | 真机连接由 ADB 提供 |
| UI 自动化 | Airtest（`airtest.core.api`） | 模板匹配 `Template`、点击 `touch`、滑动 `swipe`、`snapshot` 截图 |
| 视觉 / 语言模型 | 阿里云百炼「千问 VL」（`dashscope.MultiModalConversation`） | 单图 OCR 走 `QWEN_MODEL`，多图 RAG 走指定 `app_id` 的 Agent 应用 |
| 数据持久化 | SQLite（`boss_ai_assistant.db`） | 岗位、聊天记录、每日运行统计 |
| 异常通知 | Bark（`call_master` 调 `api.day.app`） | 主循环异常 3 次 / HR 约面试时推送 |
| 依赖注入 | 共享实例注入 | 所有模块复用同一份 `Logger / DeviceManager / SnapshotManager / OCREngine / RAGEngine` |

---

## 三、整体架构

采用**主从式 + 依赖注入**结构。入口 `main.py` 中的 `BOSSAssistant` 类在 `__init__` 阶段集中创建所有共享实例，再注入到业务模块（`JobBrowser`、`MessageReplier`），避免各模块重复 `new`、状态不一致。

```
main.py  (BOSSAssistant 主循环调度)
  ├─ config.py         配置中心（API Key、设备ID、行为阈值、提示词模板）
  ├─ logger.py         Logger：日志 + 统计 + 落库触发
  ├─ device.py         DeviceManager：ADB 封装（截图/点击/输入/重连）
  ├─ airtest_connector.py  SnapshotManager + job_JD_screenshots + 异常返回主页
  ├─ ocr_engine.py     OCREngine：截图 → 千问VL → 结构化 JSON
  ├─ RAG_engine.py     RAGEngine：多图 → 千问 Agent → 匹配判断/回复生成
  ├─ job_reader.py     parse_ai_reply：AI 文本 → 校验后的岗位列表
  ├─ intent_classifier.py  IntentClassifier：关键词意图分类（备用/早期方案）
  ├─ job_browser.py    JobBrowser：浏览 + 匹配判断 + 打招呼
  ├─ message_replier.py MessageReplier：回复 + 发简历/微信
  ├─ data_store.py     SQLite 持久层（jobs / chat_history / daily_stats）
  └─ callMaster.py     呼叫人工（Bark 推送）
```

---

## 四、运行逻辑（主循环）

入口 `BOSSAssistant.start()` 是一个**无限 `while` 循环**，每次迭代执行：

```
┌─ 1. 设备在线检查（不在线则尝试重连）
├─ 2. 检测新消息（Template 匹配消息 Tab 红点）
│     ├─ 有红点 → 进入消息列表，逐条下滑查找半截红点
│     │           → 命中 → MessageReplier.reply() 回复
│     └─ 无新消息 → 走「浏览岗位」分支
│           ├─ search_job_or_not()：若配置了 search_job 则先搜岗位
│           └─ while 循环：
│                 • 调 get_today_greet_count() 检查「当日打招呼上限」
│                 • 检查「每小时打招呼上限」
│                 • JobBrowser.browse() 上划 + 浏览 + 打招呼
│                 • 打招呼成功则跳出，回到检测消息
├─ 3. logger.print_stats()  打印面板 + 落库（daily_stats）
└─ 4. time.sleep(loop_interval) 后重复
```

**关键限流保护**（来自 `BEHAVIOR_CONFIG`）：
- `greet_per_day = 60`：每日最大打招呼数，由 `get_today_greet_count()` 实时校验。
- `max_greet_per_hour = 30`：每小时上限，由主循环 `hour_start / greet_count_this_hour` 计数。
- `greet_interval = 10`：单次打招呼最小间隔（`JobBrowser` 内部 `last_greet_time` 控制）。

**异常处理**：
- `KeyboardInterrupt` → 打印统计、保存 JSON、`sys.exit(0)`。
- 普通 `Exception` → 累计 `error_time`，打印报错栈帧（文件/行号/函数）；连续 3 次异常调 `_handle_exception` → `call_master` 推送人工。
- 每次异常后 `handle_common_exception()`：连续 `BACK` 直到回到主页，保证下一轮循环从干净状态开始。

---

## 五、两条核心业务流

### 5.1 浏览岗位 + 自动打招呼（`JobBrowser.browse`）
1. `ocr.extract_job_info()`：截图 → 千问 VL 提取前 2 个岗位卡片（标题/公司/薪资/HR/通勤）。
2. 点击进入第一个详情页。
3. 对每个岗位：`rag.extract_job_JD()` 多图识别 JD → 判断是否符合求职期望（岗位方向、薪资 ≥18K、通勤/距离约束）。
4. **不匹配** → `skip_count++`，左滑下一个。
5. **匹配** → 校验 `greet_interval` → 点「立即沟通」→ 输入 AI 生成文案 → 发送 → `greet_count++`、保存聊天记录、返回。
6. 全程每个岗位落库 `save_job_info()`，返回本次发送打招呼次数。

### 5.2 消息自动回复（`MessageReplier.reply`）
1. 局部截图提取「公司名 + HR 名」→ `get_job_by_company_hr()` 查库。
2. **命中历史岗位**：
   - 取 `chat_history` + 岗位信息 → `rag._call_rag_image` 用 `generate_reply` 提示词生成回复。
   - 解析 JSON：若 `need_resume` / `need_wechat` 则自动点对应按钮发送；若 `is_interview_invited` 则 `call_master` 推送约面试。
   - 保存 HR 消息、发送我的回复、`reply_count++`。
3. **未命中（HR 主动打招呼）**：
   - 进入岗位详情多图识别（`_check_new_job`）→ 匹配则发简历 + 打招呼文案；不匹配则礼貌拒绝文案。
   - 两种结果都落库岗位与聊天记录。

**文案生成的统一范式**：提示词模板集中在 `config.PROMPTS`，均要求模型**只返回 JSON**，代码侧统一用「截取首个 `{` 到最后一个 `}`」做容错解析，解析失败退回原始文本并记错误日志。

---

## 六、AI 集成方案

- **OCR / 单图识别**（`OCREngine`）：`dashscope.MultiModalConversation.call(model=QWEN_MODEL)`，图片以 base64 内联进 `messages.content`。
- **RAG / 多图判断**（`RAGEngine`）：同样调用上述接口，但带 `app_id`（千问 Agent 应用），用于岗位匹配判断与回复生成，支持一次传多张连续截图。
- **API 端点**：统一指向私有 MaaS 网关（阿里云百炼专属实例，端点URL见 `config.py` 的 `QWEN_BASE_URL`，仓库已脱敏）。
- **提示词工程管理**：`config.PROMPTS` 集中维护 6+ 套模板（提取岗位、提取 JD、判断新岗位、生成回复、匹配岗位、提取公司HR），是与模型交互的唯一入口，便于调优。

---

## 七、数据持久化方案（`data_store.py`）

SQLite 单库 `boss_ai_assistant.db`，共 3 张表：

| 表 | 用途 | 关键字段 |
|----|------|---------|
| `jobs` | 岗位档案（按 公司+HR 去重 upsert） | job_title/company/salary/hr_name/job_jd/job_requirements/home_distance |
| `chat_history` | 聊天记录 | job_id / message / sender(me\|hr) / timestamp |
| `daily_stats` | **每日运行统计**（本次新增） | session_id / stat_date / recorded_at / 7 项计数 / elapsed_seconds |

**本次迭代新增的能力**（本报告对应的开发工作）：
- `print_stats()` 每次被调用时自动把当前快照写入 `daily_stats`，无需改动主循环调用点。
- `get_daily_summary(date_str?)`：按天汇总，对每个 `(stat_date, session_id)` 取**自增 id 最大**的那条作为该会话当日终值，再跨会话求和 —— 正确处理「一天内多次启动 / 连续调用落在同一秒」等边界。
- `get_today_greet_count() -> int`：读取当日成功打招呼总数，主循环用它做每日上限限流；无数据返回 `0`，不返回 `None`。
- `Logger.session_id = uuid4()`：标记单次运行，作为 `daily_stats` 去重依据。

落库失败仅记 WARNING，绝不中断主流程（守护式设计）。

---

## 八、关键实现决策与权衡

1. **共享实例注入**：避免重复实例化导致截图目录 / 日志句柄 / 模型连接碎片化。
2. **JSON 容错解析**：所有 AI 返回都先 `find("{")…rfind("}")` 截取再 `json.loads`，失败兜底为 `{"raw": text}`，保证流程不崩。
3. **`daily_stats` 去重用自增 id 而非时间戳字符串**：主循环连续调用 `print_stats()` 可能落在同一秒，时间戳相同会让「取最新」失效，用单调 id 更稳。
4. **落库与统计解耦**：`daily_stats` 是「快照累加表」，汇总时按会话取终值，避免重复计算中间快照。
5. **扁平 import 约定**：全工作空间模块间一律 `from 模块名 import ...`，不加包前缀（已统一整改）。
6. **限流前置**：打招呼前同时校验「当日上限」与「每小时上限」，优先于单 job 冷却，符合平台风控直觉。

---

## 九、已知局限与后续建议

- **坐标强耦合设备**：大量 `Template(r"png/tpl*.png")` 和硬编码坐标（如 `(500,2276)`）绑定特定分辨率（1080×2400）与机型，换设备需重新采集。
- **`intent_classifier.py` 已被 RAG 方案绕过**：当前回复意图判断走 `RAGEngine` 的 `generate_reply`，关键词分类器实际未在主链路使用，可清理或改造为兜底。
- **`search_job` 默认空串**：未配置搜索词时只刷首页岗位，匹配面受限。
- **缺少人工确认环节**：README 建议的「AI 生成 → 推送微信 → 人确认 → 再发」尚未实现，当前为全自动发送。
- **`device.py` 的 `DeviceManager` 与 Airtest 全局 `touch/swipe` 混用**：部分操作走 `device.tap_by_name`，部分直接调 Airtest 全局函数，风格不统一。
- **统计口径**：`print_stats` 中的计数是进程内累积（`Logger.stats`），跨进程不连续；`daily_stats` 已能跨进程按天汇总，但实时面板只反映当前进程。

---

## 十、一句话总结

> 这是一个用 Airtest 驱动真机、用千问多模态大模型做「看屏—理解—决策—操作」闭环的 BOSS 直聘自动化助手；主循环在「回消息」与「刷岗位打招呼」之间往复，全程受每日/每小时限流保护，所有岗位、聊天与每日运行统计持久化进 SQLite，异常时通过 Bark 呼叫人工。

---

*报告结束。如需补充分模块时序图、数据库 ER 图或部署手册，可继续提出。*
